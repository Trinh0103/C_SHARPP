using System;

namespace CS003
{
    class Program
    {
        static void Main(string[] args)
        {
            /* + - * / %
            ()
            float a = 5;
            int b = 4;

            /*Console.WriteLine("a + b = {0}", a + b);
            Console.WriteLine("a - b = {0}", a - b);
            Console.WriteLine("a * b = {0}", a * b);
            Console.WriteLine("a / b = {0}", a / b);
            Console.WriteLine("a % b = {0}", a % b);
            
            float kq = 6 / (2 + 3) * 2;

            Console.WriteLine(kq);
            */

            // GAN
            // = += -= /= %=
            int x = 10;
            //x = x + 2;
            //x += 2;
            x -= 7;

            Console.WriteLine(x);
        }
    }
}